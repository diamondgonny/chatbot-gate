# Docker Compose defaults (infrastructure)
# These apply when running `docker compose up`.
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change-me
MONGO_INITDB_DATABASE=chatbot_gate

# Application runtime env lives in backend/.env (see backend/.env.example).
