/**
 * @deprecated Import from @/shared instead
 * @example import { saveUserId, getUserId, clearAuth, isAuthenticated } from "@/shared";
 */
export { saveUserId, getUserId, clearAuth, isAuthenticated } from "@/shared";
